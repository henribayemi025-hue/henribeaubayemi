# Frontend (Vite) — safe publishable values, set the real ones in Netlify.
VITE_SUPABASE_URL=https://bokwivwizghdlaedczbw.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_publishable_key

# VAPID public key for Web Push (set once VAPID keys are generated).
VITE_VAPID_PUBLIC_KEY=

# Server-side only (Supabase Edge Function secrets — never expose in the client):
#   GEMINI_API_KEY, VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT
