node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
*.local
.DS_Store
.netlify
supabase/.temp
npm-debug.log*
dist-admin
# Atelier vidéo: on IGNORE les gros fichiers produits (captures PNG, rendus
# MP4, aperçus JPEG) mais on VERSIONNE les scripts et les compositions — sans
# eux, chaque session devrait réécrire tout le pipeline de zéro.
video-work/*
!video-work/*.mjs
!video-work/*.html
!video-work/logo.svg

# Captures de vérification produites par les tests ponctuels
test-*.png
