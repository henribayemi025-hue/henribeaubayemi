# Le bac à sable de l'Atelier : l'image officielle du Sandbox SDK de
# Cloudflare, variante avec Python (Node et git y sont aussi).
# La version DOIT être la même que celle du paquet npm @cloudflare/sandbox
# (package.json) : le SDK le vérifie au démarrage.
# Rien d'autre n'est ajouté : aucune clé, aucun secret, aucun fichier de
# Finjaro dans cette image.
FROM docker.io/cloudflare/sandbox:0.12.10-python
